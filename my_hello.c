#include "library.h"

/* The bellow int main() is the basic prototype or structurw of a .c program
 * printf() - Displays information to the user in a structured and customizable manner.
 * printf() is a high level function
 * printf() - Uses the header <stdio.h>
 */

int	main()
{
	printf("Hello You\n");

	return (0);
}
